name: "Sample react CI pipeline / workflow"
on:
    push:
        branches:
            - main
            - master
jobs:
    build-and-test:
        runs-on: ubuntu-latest
        
        steps:
            - name: "Setup node JS"
              uses: actions/setup-node@v5
              with:
                node-version: '21'